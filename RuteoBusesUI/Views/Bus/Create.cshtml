@model RuteoBusesDAL.Bus

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Bus</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            @*<div class="form-group">
                <label asp-for="estadoId" class="control-label"></label>
                <input asp-for="estadoId" class="form-control" />
                <span asp-validation-for="estadoId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="estadoId" class="control-label"></label>
                <select asp-for="estadoId" class="form-control" asp-items=ViewBag.Estados>Seleccione un estado</select>
                @*
            <asp:dropdownlist id="estadoId">

                    @foreach (var item in ViewBag.Estados)
                    {
                        <asp:listitem value=@item.Value text=@item.Text></asp:listitem>
                }
            </asp:dropdownlist>*@
            </div>
            @*<div class="form-group">
                <label asp-for="estadoId" class="control-label"></label>
                @Html.DropDownListFor( @model=> @model.estadoId, (IEnumerable
                    <SelectListItem>)ViewBag.Estados)
                
                <span asp-validation-for="estadoId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="rutaId" class="control-label"></label>
                <select asp-for="rutaId" class="form-control" asp-items=ViewBag.Rutas></select>
                @*<input asp-for="rutaId" class="form-control" />*@
                <span asp-validation-for="rutaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="choferId" class="control-label"></label>
                <select asp-for="choferId" class="form-control" asp-items=ViewBag.Choferes></select>
                @*<input asp-for="choferId" class="form-control" />*@
                <span asp-validation-for="choferId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
